//big brand name(text only) sliding from left to right(when scroll down it will come out from the left side of the screen and fill the entire space of the page and when scroll up it will go back to the left side)

const FinalFooterLogo = () => {
    return (
        <div className="relative h-[240px] overflow-hidden">
            <h1 className="text-5xl font-bold text-white">Mika Creation</h1>

        </div>
    );
};
export default FinalFooterLogo;
